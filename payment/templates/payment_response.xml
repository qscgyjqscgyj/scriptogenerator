<?xml version="1.0" encoding="UTF-8"?>
<{% if aviso %}paymentAvisoResponse{% else %}checkOrderResponse{% endif %}
        performedDatetime="{{ performedDatetime }}"
        code="{{ code }}"
        invoiceId="{{ invoiceId }}"
        shopId="{{ shopId }}"
        {% if message and techMessage %}
            message="{{ message }}"
            techMessage="{{ techMessage }}"
        {% endif %}
/>